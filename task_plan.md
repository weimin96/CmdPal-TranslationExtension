# README 顶部横幅交付计划

## 目标
为项目生成一张适合 GitHub README 顶部展示的宽图，并将其放入 README 顶部，用于清晰传达项目定位：面向 PowerToys Command Palette 的 Windows 翻译扩展。

## 核心对象
- 横幅资产：用于 README 顶部展示的项目介绍图片。
- README 引用：指向项目内横幅资产的相对路径。
- 现有说明内容：README 中已有标题、徽章、截图和功能说明。

## 状态与约束
- README 已存在未提交修改，需要在当前内容基础上追加，不覆盖既有改动。
- 横幅必须落入项目目录，不能只保留在生成工具默认目录。
- README 引用应使用仓库相对路径，保证 GitHub 渲染可复现。
- 文档说明使用中文，避免引入无关英文说明。

## 阶段
- [完成] 读取项目 README、清单和当前工作区状态。
- [完成] 设计横幅内容与落盘路径。
- [完成] 生成或制作横幅资产。
- [完成] 更新 README 顶部引用。
- [完成] 验证文件存在、路径有效和工作区差异。

## 风险与处理
- README 已有用户改动：只在顶部插入横幅引用，不回滚、不重写现有内容。
- 生成图片中文字可能不稳定：优先采用可控的项目内图形资产，确保项目名和说明文字准确。

## 错误记录
| 错误 | 原因 | 处理 |
| --- | --- | --- |
| 验证命令中 `$` 变量被外层 PowerShell 提前展开 | `pwsh.exe -Command` 脚本使用了外层可解释的双引号 | 改用单引号包裹脚本并重新执行验证 |
| 浏览器截图检查误报未生成 | Edge 已输出截图，但脚本立即检查文件时未等待写入完成 | 改用等待轮询检查截图文件 |
